model:
  conv_channels: [32, 64, 128]  # one ConditionBlock per entry
  hidden_size: 256
  num_classes: 10

training:
  learning_rate: 1e-3
  max_epochs: -1
  batch_size: 128
  num_iterations: 3

dataset:
  data_dir: ./data
