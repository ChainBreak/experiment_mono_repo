[project]
name = "recursive-prediction"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "torchvision",
    "lightning",
    "ghostconfig",
    "matplotlib",
    "tensorboard",
]

[tool.uv]
package = false
