[project]
name = "goal-conditioned-rl"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "lightning",
    "ghostconfig",
    "click",
    "numpy",
    "tensorboard",
    "gymnasium[classic-control]",
    "imageio[ffmpeg]",
]

[tool.uv]
package = false
